# Driver for the invalid scan-type constraint unit test.
test-case:
  name: The invalid scan type test.
  description: Test that the specified scan type is invalid.
  content: ../content/scan-type-INVALID.xml
  expectations:
  - constraint-id: scan-type
    result: fail