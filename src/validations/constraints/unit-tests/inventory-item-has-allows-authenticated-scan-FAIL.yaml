# Driver for the invalid inventory-item-has-allows-authenticated-scan constraint unit test.
test-case:
  name: The invalid inventory-item-has-allows-authenticated-scan constraint unit test.
  description: Test that an infrastructure inventory item does not have the allows-authenticated-scan property.
  content: ../content/ssp-all-INVALID.xml
  expectations:
  - constraint-id: inventory-item-has-allows-authenticated-scan
    result: fail