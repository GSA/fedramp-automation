# Driver for the valid inventory-item-has-allows-authenticated-scan constraint unit test.
test-case:
  name: The valid inventory-item-has-allows-authenticated-scan constraint unit test.
  description: Test that every infrastructure inventory item has the allows-authenticated-scan property.
  content: ../content/ssp-all-VALID.xml
  expectations:
  - constraint-id: inventory-item-has-allows-authenticated-scan
    result: pass