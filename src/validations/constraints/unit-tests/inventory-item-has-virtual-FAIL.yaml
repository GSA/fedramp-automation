# Driver for the invalid inventory-item-has-virtual constraint unit test.
test-case:
  name: The invalid inventory-item-has-virtual constraint unit test.
  description: Test that an SSP inventory item does not have the virtual property.
  content: ../content/ssp-all-INVALID.xml
  expectations:
  - constraint-id: inventory-item-has-virtual
    result: fail