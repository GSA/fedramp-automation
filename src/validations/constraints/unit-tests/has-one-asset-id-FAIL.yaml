# Driver for the invalid has-one-asset-id constraint unit test.
test-case:
  name: The invalid has-one-asset-id constraint unit test.
  description: Test that an SSP inventory item has more than one asset identifier.
  content: ../content/ssp-all-INVALID.xml
  expectations:
  - constraint-id: has-one-asset-id
    result: fail