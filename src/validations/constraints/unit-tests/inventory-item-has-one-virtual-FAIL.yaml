# Driver for the invalid inventory-item-has-one-virtual constraint unit test.
test-case:
  name: The invalid inventory-item-has-one-virtual constraint unit test.
  description: Test that an SSP inventory item has more than one virtual property.
  content: ../content/ssp-all-INVALID.xml
  expectations:
  - constraint-id: inventory-item-has-one-virtual
    result: fail