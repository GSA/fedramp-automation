# Driver for the valid inventory-item-has-virtual constraint unit test.
test-case:
  name: The valid inventory-item-has-virtual constraint unit test.
  description: Test that every SSP inventory item has the virtual property.
  content: ../content/ssp-all-VALID.xml
  expectations:
  - constraint-id: inventory-item-has-virtual
    result: pass