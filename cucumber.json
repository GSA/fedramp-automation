{
  "default": {
    "requireModule": ["ts-node/register"],
    "import": ["features/**/*.ts"],
    "format": [
      ["junit:reports/junit-{feature}.xml"]
    ],
    "retry": 2,
    "retryTagFilter": "@flaky"
  }
}