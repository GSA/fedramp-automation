{
  "default": {
    "requireModule": ["ts-node/register"],
    "import": ["features/**/*.ts"],
    "format": [
      ["junit", "reports/constraints.xml"]
    ],
    "retry": 2,
    "retryTagFilter": "@flaky"
  }
}